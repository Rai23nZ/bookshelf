diff --git a/src/BookApp.dc.html b/src/BookApp.dc.html
index 24c2269..f5ab337 100644
--- a/src/BookApp.dc.html
+++ b/src/BookApp.dc.html
@@ -146,9 +146,10 @@ input[type=range]{accent-color:var(--color-accent)}
 <div style="position:relative;aspect-ratio:2/3;border-radius:14px;overflow:hidden;background:{{ b.ph }};box-shadow:var(--shadow-md);margin-bottom:9px">
 <div style="position:absolute;inset:0;padding:12px;display:flex;flex-direction:column;justify-content:space-between">
 <div style="font-family:var(--font-heading);font-size:14px;line-height:1.16;color:rgba(255,255,255,.95)">{{ b.short }}</div>
-<div style="font-size:9px;letter-spacing:.08em;text-transform:uppercase;color:rgba(255,255,255,.72)">{{ b.authorShort }}</div>
+<div style="font-size:9px;letter-spacing:.08em;text-transform:uppercase;color:rgba(255,255,255,.72);padding-right:52px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">{{ b.authorShort }}</div>
 </div>
 <sc-if value="{{ b.hasCover }}" hint-placeholder-val="{{ false }}"><img src="{{ b.coverUrl }}" data-bid="{{ b.id }}" onError="{{ hideImg }}" alt="" style="position:absolute;inset:0;width:100%;height:100%;object-fit:cover"></sc-if>
+<sc-if value="{{ b.rated }}" hint-placeholder-val="{{ true }}"><div style="position:absolute;right:5px;bottom:5px;height:19px;padding:0 7px;border-radius:999px;background:rgba(32,30,29,.78);color:#fff;font-size:10px;font-weight:700;display:flex;align-items:center;gap:3px">{{ b.rating }} ★</div></sc-if>
 </div>
 <div style="font-family:var(--font-heading);font-size:13.5px;line-height:1.2;margin-bottom:3px">{{ b.short }}</div>
 <div style="font-size:11.5px;color:var(--color-neutral-600);margin-bottom:6px">{{ b.authorShort }}</div>
@@ -187,9 +188,10 @@ input[type=range]{accent-color:var(--color-accent)}
 <div style="position:relative;aspect-ratio:2/3;border-radius:14px;overflow:hidden;background:{{ b.ph }};box-shadow:var(--shadow-md);margin-bottom:9px">
 <div style="position:absolute;inset:0;padding:12px;display:flex;flex-direction:column;justify-content:space-between">
 <div style="font-family:var(--font-heading);font-size:14px;line-height:1.16;color:rgba(255,255,255,.95)">{{ b.short }}</div>
-<div style="font-size:9px;letter-spacing:.08em;text-transform:uppercase;color:rgba(255,255,255,.72)">{{ b.authorShort }}</div>
+<div style="font-size:9px;letter-spacing:.08em;text-transform:uppercase;color:rgba(255,255,255,.72);padding-right:52px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">{{ b.authorShort }}</div>
 </div>
 <sc-if value="{{ b.hasCover }}" hint-placeholder-val="{{ false }}"><img src="{{ b.coverUrl }}" data-bid="{{ b.id }}" onError="{{ hideImg }}" alt="" style="position:absolute;inset:0;width:100%;height:100%;object-fit:cover"></sc-if>
+<sc-if value="{{ b.rated }}" hint-placeholder-val="{{ true }}"><div style="position:absolute;right:5px;bottom:5px;height:19px;padding:0 7px;border-radius:999px;background:rgba(32,30,29,.78);color:#fff;font-size:10px;font-weight:700;display:flex;align-items:center;gap:3px">{{ b.rating }} ★</div></sc-if>
 </div>
 <div style="font-family:var(--font-heading);font-size:13.5px;line-height:1.2;margin-bottom:3px">{{ b.short }}</div>
 <div style="font-size:11.5px;color:var(--color-neutral-600);margin-bottom:6px">{{ b.authorShort }}</div>
@@ -217,9 +219,10 @@ input[type=range]{accent-color:var(--color-accent)}
 <div style="position:relative;width:96px;height:144px;border-radius:9px;overflow:hidden;background:{{ b.ph }};box-shadow:var(--shadow-sm);margin-bottom:7px">
 <div style="position:absolute;inset:0;padding:9px;display:flex;flex-direction:column;justify-content:space-between">
 <div style="font-family:var(--font-heading);font-size:11px;line-height:1.18;color:rgba(255,255,255,.95)">{{ b.short }}</div>
-<div style="font-size:8px;letter-spacing:.08em;text-transform:uppercase;color:rgba(255,255,255,.72)">{{ b.authorShort }}</div>
+<div style="font-size:8px;letter-spacing:.08em;text-transform:uppercase;color:rgba(255,255,255,.72);padding-right:44px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">{{ b.authorShort }}</div>
 </div>
 <sc-if value="{{ b.hasCover }}" hint-placeholder-val="{{ false }}"><img src="{{ b.coverUrl }}" data-bid="{{ b.id }}" onError="{{ hideImg }}" alt="" style="position:absolute;inset:0;width:100%;height:100%;object-fit:cover"></sc-if>
+<sc-if value="{{ b.rated }}" hint-placeholder-val="{{ true }}"><div style="position:absolute;right:5px;bottom:5px;height:19px;padding:0 7px;border-radius:999px;background:rgba(32,30,29,.78);color:#fff;font-size:10px;font-weight:700;display:flex;align-items:center;gap:3px">{{ b.rating }} ★</div></sc-if>
 </div>
 <div style="font-size:11px;line-height:1.25;color:var(--color-neutral-700);overflow:hidden;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical">{{ b.short }}</div>
 </div>
@@ -525,6 +528,20 @@ Download CSV for Goodreads
 <div style="font-size:12.5px;line-height:1.6;color:var(--color-neutral-700)">Goodreads’ own column set, comma-separated, UTF-8. Decimal ratings are rounded to whole stars; fields with commas are quoted; each row keeps its shelf (read, currently-reading, to-read) so nothing lands on the wrong pile. Re-importing the same file is safe.</div>
 </div>
 
+<h2 style="font-family:var(--font-heading);font-size:22px;margin:0 0 4px">Import from CSV</h2>
+<div style="font-size:13px;color:var(--color-neutral-600);margin-bottom:14px">Read in the file the button above produces, or a library export from Goodreads itself.</div>
+<div style="border-radius:24px;background:var(--color-neutral-100);border:1px solid var(--color-neutral-200);padding:16px;margin-bottom:22px">
+<div style="font-size:10px;letter-spacing:.14em;text-transform:uppercase;color:var(--color-neutral-600);margin-bottom:8px">How to apply it</div>
+<div style="display:flex;gap:8px;margin-bottom:14px">
+<sc-for list="{{ csvModes }}" as="m" hint-placeholder-count="2">
+<button onClick="{{ m.pick }}" style="flex:1;height:38px;border-radius:999px;border:1px solid {{ m.bd }};background:{{ m.bg }};color:{{ m.fg }};font-family:var(--font-body);font-weight:600;font-size:11.5px;cursor:pointer">{{ m.label }}</button>
+</sc-for>
+</div>
+<div style="font-size:10px;letter-spacing:.14em;text-transform:uppercase;color:var(--color-neutral-600);margin-bottom:8px">Choose a CSV file</div>
+<input type="file" accept="text/csv,.csv" onChange="{{ onImportCsvFile }}" style="width:100%;font-family:var(--font-body);font-size:12px;color:var(--color-neutral-700)">
+<div style="font-size:11.5px;line-height:1.6;color:var(--color-neutral-600);margin-top:10px">Each row is matched to a book by ISBN, or by title and author. {{ csvModeHint }}</div>
+</div>
+
 <h2 style="font-family:var(--font-heading);font-size:22px;margin:0 0 4px">Backup</h2>
 <div style="font-size:13px;color:var(--color-neutral-600);margin-bottom:14px">A full copy — ratings, notes, shelves <em>and</em> reading progress. The CSV above deliberately keeps Goodreads’ columns, so it can’t carry your progress percentages; this file can.</div>
 <div style="border-radius:24px;background:var(--color-neutral-100);border:1px solid var(--color-neutral-200);padding:16px;margin-bottom:22px">
@@ -912,7 +929,7 @@ class Component extends DCLogic {
     lastProgressId: null, toast: '', readDays: [],
     syncCode: '', syncStatus: '', syncUrl: '', lastSync: null,
     addTab: 'search', addQuery: '', addExternal: [], addExternalLoading: false,
-    mTitle: '', mAuthor: '', mIsbn: '', mShelf: 'to-read'
+    mTitle: '', mAuthor: '', mIsbn: '', mShelf: 'to-read', csvMode: 'merge'
   };
 
   covers = {};
@@ -1272,6 +1289,224 @@ class Component extends DCLogic {
     reader.readAsText(file);
   }
 
+  // ── Import from a Goodreads-style CSV ───────────────────────────────────
+  // Two sources look the same to this: the file "Download CSV for
+  // Goodreads" produces, and a real library export from Goodreads itself.
+  // The two don't use identical headers (this app writes "Shelves", real
+  // Goodreads writes "Exclusive Shelf"), so every field is read through a
+  // short alias list rather than one exact name.
+  csvCols() {
+    return {
+      title: ['Title'], author: ['Author', 'Author l-f'],
+      isbn13: ['ISBN13', 'ISBN-13'], isbn: ['ISBN'], isbn10: ['ISBN10', 'ISBN-10'],
+      asin: ['ASIN'],
+      rating: ['My Rating', 'Rating'], avgRating: ['Average Rating'],
+      pages: ['Number of Pages', 'Pages'],
+      dateRead: ['Date Read'], dateAdded: ['Date Added'],
+      shelf: ['Exclusive Shelf', 'Shelves', 'Shelf', 'Status'],
+      series: ['Series', 'Bookshelves'], note: ['My Review', 'Private Notes', 'Review', 'Notes']
+    };
+  }
+
+  // A real parser rather than a `.split(',')` — fields with a comma (or a
+  // semicolon, see below) are quoted, quotes inside a field are doubled,
+  // and ISBNs are wrapped as ="1234567890" (Excel's trick for keeping a
+  // leading zero as text).
+  //
+  // The delimiter isn't always a comma: spreadsheet apps in locales where
+  // comma is the decimal separator (common with a Cyrillic-locale Excel,
+  // for instance) export CSVs with semicolons instead, so it's sniffed
+  // from the header row rather than assumed.
+  parseCsv(text) {
+    const s = text.charCodeAt(0) === 0xFEFF ? text.slice(1) : text;
+    const firstLine = s.split(/\r?\n/, 1)[0] || '';
+    const commas = (firstLine.match(/,/g) || []).length;
+    const semis = (firstLine.match(/;/g) || []).length;
+    const delim = semis > commas ? ';' : ',';
+    const rows = [];
+    let row = [], field = '', inQ = false;
+    for (let i = 0; i < s.length; i++) {
+      const c = s[i];
+      if (inQ) {
+        if (c === '"' && s[i + 1] === '"') { field += '"'; i++; }
+        else if (c === '"') inQ = false;
+        else field += c;
+      } else if (c === '"') inQ = true;
+      else if (c === delim) { row.push(field); field = ''; }
+      else if (c === '\n') { row.push(field); rows.push(row); row = []; field = ''; }
+      else if (c !== '\r') field += c;
+    }
+    if (field || row.length) { row.push(field); rows.push(row); }
+    if (!rows.length) return [];
+    const header = rows[0].map(h => h.trim());
+    // Drop fully-blank rows (a trailing newline reads as one otherwise).
+    return rows.slice(1).filter(r => r.some(c => c.trim() !== '')).map(r => {
+      const o = {};
+      header.forEach((h, i) => { o[h] = (r[i] || '').trim(); });
+      return o;
+    });
+  }
+
+  cleanIsbn(v) { return String(v || '').replace(/^="?/, '').replace(/"+$/, '').replace(/[^0-9Xx]/g, ''); }
+
+  // "2023/05/14" (Goodreads) or "2023-05-14" (this app) -> "2023-05-14".
+  normDate(v) {
+    const m = String(v || '').trim().match(/^(\d{4})[\/-](\d{1,2})[\/-](\d{1,2})/);
+    return m ? m[1] + '-' + m[2].padStart(2, '0') + '-' + m[3].padStart(2, '0') : '';
+  }
+
+  // Different exporters disagree about which column ("ISBN", "ISBN10",
+  // "ISBN13") holds which code — one export seen in the wild puts the
+  // 13-digit code under a plain "ISBN" header with no "ISBN13" column at
+  // all. Rather than trust the header name literally, every ISBN-ish
+  // column is read and then sorted by the length of its cleaned value.
+  pickIsbns(row, cols) {
+    const candidates = [
+      this.cleanIsbn(this.pickCol(row, cols.isbn13)),
+      this.cleanIsbn(this.pickCol(row, cols.isbn)),
+      this.cleanIsbn(this.pickCol(row, cols.isbn10))
+    ].filter(Boolean);
+    const isbn13 = candidates.find(v => v.replace(/[^0-9]/g, '').length === 13) || '';
+    const isbn = candidates.find(v => v.length === 10) || '';
+    return { isbn13, isbn };
+  }
+
+  // Loose match key: case, accents and punctuation shouldn't decide whether
+  // a CSV row is "the same book" as one already on the shelf.
+  normText(v) {
+    return String(v || '').toLowerCase().normalize('NFKD').replace(/[^\p{L}\p{N}]+/gu, ' ').trim();
+  }
+
+  pickCol(row, keys) {
+    for (const k of keys) { if (row[k]) return row[k]; }
+    return '';
+  }
+
+  // Parses the CSV and matches each row against the shelf by ISBN13, then
+  // ISBN, then title+author. `extraPool` is an optional second set of books
+  // to also match against besides the pristine catalog — mergeCsv() passes
+  // its own already-imported extras here so that re-running the same file
+  // updates them in place instead of piling up duplicates; importCsv()
+  // leaves it out and matches only against the catalog, since a "replace"
+  // rebuilds the extras list from this file's rows anyway. Shared by both
+  // apply modes below; what happens with the result is the only difference
+  // between them.
+  buildCsvSlim(text, extraPool) {
+    if (!this._catalog) { this.say('Still loading — try again in a moment'); return null; }
+    const rows = this.parseCsv(text);
+    if (!rows.length) { this.say('That file has no rows to import'); return null; }
+    const cols = this.csvCols();
+    const shelfIds = SHELVES.map(sh => sh.id);
+    const byIsbn13 = {}, byIsbn = {}, byTitleAuthor = {};
+    const index = b => {
+      if (b.isbn13) byIsbn13[b.isbn13] = b;
+      if (b.isbn) byIsbn[b.isbn] = b;
+      const key = this.normText(b.title) + '|' + this.normText(b.author);
+      if (key !== '|') byTitleAuthor[key] = b;
+    };
+    this._catalog.forEach(index);
+    // Indexed after the catalog, so a collision resolves to the user's own
+    // extra rather than a same-titled catalog book.
+    (extraPool || []).forEach(index);
+
+    const slim = [];
+    let matched = 0, added = 0, skipped = 0;
+    rows.forEach((row, i) => {
+      const title = this.pickCol(row, cols.title).trim();
+      if (!title) { skipped++; return; }
+      const author = this.pickCol(row, cols.author).trim() || 'Unknown';
+      const { isbn13, isbn } = this.pickIsbns(row, cols);
+      // parseFloat, not parseInt — ratings like "4.30" carry a fraction the
+      // app displays and bars its progress-style rating meter on.
+      const ratingRaw = parseFloat(this.pickCol(row, cols.rating));
+      const myRating = Number.isFinite(ratingRaw) ? Math.min(5, Math.max(0, ratingRaw)) : 0;
+      const dateRead = this.normDate(this.pickCol(row, cols.dateRead));
+      const dateAdded = this.normDate(this.pickCol(row, cols.dateAdded)) || new Date().toISOString().slice(0, 10);
+      const note = this.pickCol(row, cols.note);
+      let shelf = this.pickCol(row, cols.shelf).trim().toLowerCase();
+      // No usable shelf column (a plain personal history export, say) —
+      // infer it from whatever else the row does have.
+      if (!shelfIds.includes(shelf)) shelf = (dateRead || myRating) ? 'read' : 'to-read';
+
+      const found = (isbn13 && byIsbn13[isbn13]) || (isbn && byIsbn[isbn])
+        || byTitleAuthor[this.normText(title) + '|' + this.normText(author)];
+
+      if (found) {
+        matched++;
+        slim.push({ id: found.id, myRating, shelf, dateRead, note });
+      } else {
+        added++;
+        const pagesRaw = parseInt(this.pickCol(row, cols.pages), 10);
+        const avgRaw = parseFloat(this.pickCol(row, cols.avgRating));
+        const nb = {
+          id: Date.now() + i, title, author,
+          series: this.pickCol(row, cols.series), isbn, isbn13,
+          asin: this.pickCol(row, cols.asin),
+          myRating, avgRating: Number.isFinite(avgRaw) ? avgRaw : null,
+          pages: Number.isFinite(pagesRaw) ? pagesRaw : null,
+          dateRead, dateAdded, shelf,
+          readCount: shelf === 'read' ? 1 : 0, note, _new: true
+        };
+        slim.push({ id: nb.id, myRating, shelf, dateRead, note, _new: nb });
+      }
+    });
+
+    if (!slim.length) { this.say('No usable rows in that file — need at least a title in each'); return null; }
+    return { slim, matched, added, skipped };
+  }
+
+  csvResultMsg(r, tail) {
+    return 'Imported ' + (r.matched + r.added) + ' books — ' + r.matched + ' matched, ' + r.added + ' new'
+      + (r.skipped ? ', ' + r.skipped + ' skipped' : '') + tail;
+  }
+
+  // "Replace shelf": matches only against the pristine catalog, then goes
+  // through the same restoreSnapshot() path as a JSON backup — so, same as
+  // that, it rebuilds the whole shelf from this file rather than layering
+  // on top of what's there. Meant for a shelf that has nothing on it yet.
+  importCsv(text) {
+    const r = this.buildCsvSlim(text);
+    if (!r) return false;
+    const ok = this.restoreSnapshot({
+      v: 1, slim: r.slim, progress: {}, goal: this.state.goal, shelf: this.state.shelf,
+      lastProgressId: null, readDays: this.state.readDays || []
+    }, 'CSV file');
+    if (ok) this.say(this.csvResultMsg(r, ''));
+    return ok;
+  }
+
+  // "Add to shelf": also matches against whatever the shelf has already
+  // picked up from an earlier CSV import, and only touches the books this
+  // file actually mentions — everything else on the shelf is left exactly
+  // as it is.
+  mergeCsv(text) {
+    const extras = (this.state.books || []).filter(b => b._new);
+    const r = this.buildCsvSlim(text, extras);
+    if (!r) return false;
+    const byId = {};
+    r.slim.forEach(x => { byId[x.id] = x; });
+    const newExtras = r.slim.filter(x => x._new).map(x => x._new);
+    const books = (this.state.books || []).map(b => byId[b.id]
+      ? { ...b, myRating: byId[b.id].myRating, shelf: byId[b.id].shelf, dateRead: byId[b.id].dateRead, note: byId[b.id].note || '' }
+      : b);
+    this.setState({ books: [...newExtras, ...books] });
+    this.say(this.csvResultMsg(r, ' — rest of your shelf untouched'));
+    return true;
+  }
+
+  onImportCsvFile(e) {
+    const file = e.target && e.target.files && e.target.files[0];
+    if (!file) return;
+    const mode = this.state.csvMode === 'replace' ? 'replace' : 'merge';
+    const reader = new FileReader();
+    reader.onload = () => {
+      const text = String(reader.result);
+      if (mode === 'replace') this.importCsv(text); else this.mergeCsv(text);
+    };
+    reader.onerror = () => this.say('Could not read that file');
+    reader.readAsText(file);
+  }
+
   // ── Cloud sync (Cloudflare Pages Function + KV) ────────────────────────
   get syncCodeKey() { return 'bookshelf.synccode.v1'; }
 
@@ -1872,15 +2107,20 @@ class Component extends DCLogic {
   }
 
   exportCsv() {
-    const H = ['Title', 'Author', 'ISBN', 'ISBN13', 'My Rating', 'Average Rating', 'Number of Pages', 'Date Read', 'Date Added', 'Shelves', 'Bookshelves', 'My Review'];
+    const H = ['Title', 'Author', 'ISBN', 'ISBN13', 'ASIN', 'My Rating', 'Average Rating', 'Number of Pages', 'Date Read', 'Date Added', 'Shelves', 'Bookshelves', 'Series', 'My Review'];
     const q = v => {
       const s = v == null ? '' : String(v);
       return /[",\n]/.test(s) ? '"' + s.replace(/"/g, '""') + '"' : s;
     };
     const rows = (this.state.books || []).map(b => [
-      b.title, b.author, b.isbn ? '="' + b.isbn + '"' : '', b.isbn13 ? '="' + b.isbn13 + '"' : '',
+      b.title, b.author, b.isbn ? '="' + b.isbn + '"' : '', b.isbn13 ? '="' + b.isbn13 + '"' : '', b.asin || '',
       b.myRating ? Math.round(b.myRating) : '', b.avgRating || '', b.pages || '',
-      b.dateRead || '', b.dateAdded || '', b.shelf, b.series || '', b.note || ''
+      b.dateRead || '', b.dateAdded || '', b.shelf,
+      // "Bookshelves" is left empty — it's a Goodreads-import convenience
+      // column this app doesn't use for anything, and used to be
+      // repurposed to carry series here, which just meant a re-import
+      // mislabeled series as a shelf tag. Series has its own column now.
+      '', b.series || '', b.note || ''
     ].map(q).join(','));
     const csv = '\ufeff' + [H.join(','), ...rows].join('\r\n');
     const url = URL.createObjectURL(new Blob([csv], { type: 'text/csv;charset=utf-8' }));
@@ -1923,6 +2163,10 @@ class Component extends DCLogic {
             progress: { ...s.progress, [b.id]: 100 },
             lastProgressId: null
           }));
+          // Straight into the book's own card rather than back to the shelf
+          // — finishing is exactly when a rating/review is top of mind, and
+          // otherwise it takes a second trip to open the book and fill it in.
+          this.pushScreen({ screen: 'book', selId: b.id });
           this.say('Finished — moved to Read, dated today');
         }
       };
@@ -2272,6 +2516,14 @@ class Component extends DCLogic {
       doExport: () => this.exportCsv(),
       doBackup: () => this.doBackup(),
       onRestoreFile: e => this.onRestoreFile(e),
+      onImportCsvFile: e => this.onImportCsvFile(e),
+      csvModes: [
+        { id: 'merge', label: 'Add to shelf', ...this.pillStyle(s.csvMode !== 'replace'), pick: () => this.setState({ csvMode: 'merge' }) },
+        { id: 'replace', label: 'Replace shelf', ...this.pillStyle(s.csvMode === 'replace'), pick: () => this.setState({ csvMode: 'replace' }) }
+      ],
+      csvModeHint: s.csvMode === 'replace'
+        ? 'Rebuilds the whole shelf from this file. Anything not in it — including books added since your last CSV import — reverts to how it started.'
+        : 'Matches rows to what\u2019s already on the shelf and updates those; anything unmatched is added. The rest of the shelf is left alone.',
       syncCode: s.syncCode,
       syncStatus: s.syncStatus || this.syncLabel(s.lastSync) || 'Not synced yet on this device.',
       syncBlurb: 'Keeps your shelf on a server, so it survives clearing the browser and can be picked up on another device. Same code = same shelf.',
